# ─────────────────────────────────────────────
#  Smutscrape — full dependency list
#  Install with:  pip install -r requirements.txt
#
#  NOTE: tkinter (used by the GUI) ships with
#  Python on Windows/macOS. On Linux install it
#  separately with your package manager:
#    Debian/Ubuntu:  sudo apt install python3-tk
#    Fedora/RHEL:    sudo dnf install python3-tkinter
#    Arch:           sudo pacman -S tk
# ─────────────────────────────────────────────

# ── HTTP / scraping ───────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.1.0
cloudscraper>=1.2.71
feedparser>=6.0.11

# ── YAML config ───────────────────────────────
pyyaml>=6.0.1

# ── Video downloading ─────────────────────────
yt-dlp>=2025.02.19

# ── Selenium (sites that need JS rendering) ───
selenium>=4.18.1
webdriver-manager>=4.0.1

# ── Progress bars ─────────────────────────────
tqdm>=4.66.0

# ── Logging & terminal output ─────────────────
loguru>=0.7.2
termcolor>=2.5.0
rich>=13.7.0
art>=6.4

# ── Network storage (optional, SMB / WebDAV) ──
smbprotocol>=1.13.0
pysmb>=1.2.9
webdavclient3>=3.14.6

# ── REST API server (optional) ────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.4.0
